import { getProfile } from "routes/_app/settings/-queries/queries/getProfile"

/******************************************************************
 *  QUERY TREE START
 ******************************************************************/
export const settingsQuery = Object.freeze({
  getProfile,
})
